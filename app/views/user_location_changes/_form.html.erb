<%= form_for(@user_location_change) do |f| %>
  <% if @user_location_change.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_location_change.errors.count, "error") %> prohibited this user_location_change from being saved:</h2>

      <ul>
      <% @user_location_change.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :location_id %><br>
    <%= f.text_field :location_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
