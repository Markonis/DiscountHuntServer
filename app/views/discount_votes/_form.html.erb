<%= form_for(@discount_vote) do |f| %>
  <% if @discount_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discount_vote.errors.count, "error") %> prohibited this discount_vote from being saved:</h2>

      <ul>
      <% @discount_vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :discount_id %><br>
    <%= f.text_field :discount_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
